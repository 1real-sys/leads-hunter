/** Datas `LocalDateTime` retornadas pelo backend, sem offset ou fuso horário. */
export type LocalDateTimeString = string;

/** Timestamp UTC usado no envelope de erro da API. */
export type InstantString = string;
